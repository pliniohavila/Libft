#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "libft.h"

#define SIZE 30

int     main(void)
{
    // char    *buf1;
    // char    *buf2;

    // memset(buf1, 0, SIZE);
    // memset(buf2, 0, SIZE);
    // strcpy(buf2, " vtr");
    // buf2 = ft_split(buf1, 32);
    // for (int i = 0; buf2[i] != NULL; i++)
    //     printf("%s - ", buf2[i]);
    printf("\n");
    return (0);
}

